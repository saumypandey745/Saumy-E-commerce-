[build]
  # Netlify me site ka Base directory = frontend/apps/seller-hub set karo
  command = "cd ../../.. && pnpm install --no-frozen-lockfile && pnpm --filter seller-hub build"
  publish = ".next"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://your-api-gateway.com"
  NODE_VERSION = "18"
  PNPM_VERSION = "8"

[[plugins]]
  package = "@netlify/plugin-nextjs"
