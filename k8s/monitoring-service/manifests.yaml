apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: monitoring-service
  name: monitoring-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: monitoring-service
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: monitoring-service
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - monitoring-service
              topologyKey: kubernetes.io/hostname
            weight: 100
      containers:
      - env:
        - name: PORT
          value: '8011'
        image: registry.example.com/ecommerce/monitoring-service:latest
        imagePullPolicy: Always
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/sh
              - -c
              - sleep 5
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 10
          periodSeconds: 15
        name: monitoring-service
        ports:
        - containerPort: 8011
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 256Mi
        startupProbe:
          failureThreshold: 12
          httpGet:
            path: /health
            port: 8011
          initialDelaySeconds: 5
          periodSeconds: 10
      terminationGracePeriodSeconds: 60
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: monitoring-service
  name: monitoring-service
spec:
  ports:
  - port: 8011
    targetPort: 8011
  selector:
    app: monitoring-service
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: monitoring-service
spec:
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  - resource:
      name: memory
      target:
        averageUtilization: 80
        type: Utilization
    type: Resource
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: monitoring-service
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: monitoring-service
spec:
  minAvailable: 50%
  selector:
    matchLabels:
      app: monitoring-service
